% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_r95p.R, R/calculate_r99ptot.R
\name{calculate_r99ptot}
\alias{calculate_r99ptot}
\title{Calculate R99pTOT (Contribution of Extremely Wet Days)}
\usage{
calculate_r99ptot(
  nc_input,
  start_date = NULL,
  baseline_range = c(1981, 2010),
  region = NULL,
  country = NULL,
  region_name = NULL,
  wet_threshold = 1,
  plot_result = TRUE
)

calculate_r99ptot(
  nc_input,
  start_date = NULL,
  baseline_range = c(1981, 2010),
  region = NULL,
  country = NULL,
  region_name = NULL,
  wet_threshold = 1,
  plot_result = TRUE
)
}
\arguments{
\item{nc_input}{Character path to NetCDF file OR a SpatRaster object.}

\item{start_date}{Character/Date. Start date (e.g. "1981-01-01").}

\item{baseline_range}{Numeric Vector. Baseline years (e.g., c(1981, 2010)).}

\item{region}{(Optional) Shapefile path or SpatVector to mask data.}

\item{country}{(Optional) Character. ISO3 code (e.g., "ETH") to auto-download boundary.}

\item{region_name}{(Optional) Character. Admin region name (e.g., "Oromia") to filter.}

\item{wet_threshold}{Numeric. Minimum rain (mm) for wet days. Default 1.}

\item{plot_result}{Logical. If TRUE, plots the results.}

\item{save_result}{Logical. If TRUE, saves the output as a .tif file.}

\item{output_dir}{Character. Directory to save the file.}

\item{output_name}{Character. Prefix for the saved file name.}
}
\value{
A SpatRaster object containing the R99pTOT values (\%) for each year.

A SpatRaster object containing the R99pTOT values (\%) for each year.
}
\description{
Calculates the percentage (\%) of total annual precipitation that comes from
days exceeding the 99th percentile (Extremely Wet Days).

Calculates the percentage (\%) of total annual precipitation that comes from
days exceeding the 99th percentile (Extremely Wet Days).
}
\details{
The R99pTOT index represents the contribution of extreme events to the total water budget:
\deqn{R99pTOT = \left( \frac{R99p}{PRCPTOT} \right) \times 100}

Where:
\itemize{
\item \strong{R99p}: Annual sum of precipitation from days > 99th percentile.
\item \strong{PRCPTOT}: Annual total precipitation from wet days (> 1mm).
}

The R99pTOT index represents the contribution of extreme events to the total water budget:
\deqn{R99pTOT = \left( \frac{R99p}{PRCPTOT} \right) \times 100}

Where:
\itemize{
\item \strong{R99p}: Annual sum of precipitation from days > 99th percentile.
\item \strong{PRCPTOT}: Annual total precipitation from wet days (> 1mm).
}
}
