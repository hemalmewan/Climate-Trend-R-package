% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_rxdday.R
\name{calculate_rxdday}
\alias{calculate_rxdday}
\title{Calculate Rxdday (Maximum x-day Precipitation)}
\usage{
calculate_rxdday(
  nc_input,
  start_date = NULL,
  window_size = 1,
  timescale = c("monthly", "annual"),
  region = NULL,
  country = NULL,
  region_name = NULL,
  plot_result = TRUE,
  save_result = FALSE,
  output_dir = "Output_Maps",
  output_name = "Rxdday_Index"
)
}
\arguments{
\item{nc_input}{Character path to NetCDF file OR a SpatRaster object.}

\item{start_date}{Character/Date. Start date (e.g. "1981-01-01").}

\item{window_size}{Integer. The number of days for the rolling sum. Default 1 (Rx1day). Use 5 for Rx5day.}

\item{timescale}{Character vector. "monthly", "annual", or both.}

\item{region}{(Optional) Shapefile path or SpatVector to mask data.}

\item{country}{(Optional) Character. ISO3 code (e.g., "ETH") to auto-download boundary.}

\item{region_name}{(Optional) Character. Admin region name (e.g., "Oromia") to filter.}

\item{plot_result}{Logical. If TRUE, plots the results.}

\item{save_result}{Logical. If TRUE, saves the output as a .tif file.}

\item{output_dir}{Character. Directory to save the file.}

\item{output_name}{Character. Prefix for the saved file name.}
}
\value{
A named list containing 'monthly' and/or 'annual' SpatRasters.
}
\description{
Calculates the maximum precipitation amount accumulated over a moving window of x days.
Common uses are Rx1day (window=1) and Rx5day (window=5).
}
\details{
The Rxdday index represents the highest rainfall accumulation over a specified duration:
\deqn{Rx\text{day} = \max(\text{rolling\_sum}(\text{Precip}, \text{window}))}

Where:
\itemize{
\item \strong{window}: The number of consecutive days (e.g., 1 or 5).
\item \strong{rolling_sum}: The sum of precipitation for each overlapping window.
}
}
