% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_wet_dry_timing.R
\name{calculate_wet_dry_timing}
\alias{calculate_wet_dry_timing}
\title{Calculate Spatial Timing for Wet and Dry Seasons}
\usage{
calculate_wet_dry_timing(
  nc_input,
  start_date = NULL,
  region_name = NULL,
  country = NULL,
  wet_threshold = 20,
  dry_threshold = 5,
  window_size = 10,
  save_path = NULL,
  plot_result = TRUE
)
}
\arguments{
\item{nc_input}{Character path to NetCDF file OR a SpatRaster object.}

\item{start_date}{Character/Date. Start date (e.g. "1981-01-01").}

\item{region_name}{(Optional) Character. Admin region name (e.g., "Amhara") to filter.}

\item{country}{(Optional) Character. ISO3 code (e.g., "ETH").}

\item{wet_threshold}{Numeric. The 10-day mm sum required to trigger the Wet Season. Default is 20.}

\item{dry_threshold}{Numeric. The 10-day mm sum required to trigger the Dry Season. Default is 5.}

\item{window_size}{Numeric. The number of days in the rolling window. Default is 10.}

\item{save_path}{(Optional) Character. File path to save the output raster.}

\item{plot_result}{Logical. If TRUE, plots the four spatial maps for the first year.}
}
\value{
A SpatRaster stack containing four layers per year:
\code{Wet_Onset}, \code{Wet_Retreat}, \code{Dry_Onset}, \code{Dry_Retreat}.
}
\description{
Calculates the exact Day of the Year (DOY) for the onset and retreat of both
the Wet Season and the Dry Season using a rolling 10-day rainfall sum.
}
\details{
This function evaluates the occurrence of distinct seasons using a rolling sum
approach to filter out erratic "false start" rainfall events.

Mathematically, the function computes a rolling sum \eqn{S_t} of daily precipitation \eqn{P}
over a window of \eqn{w} days (default \eqn{w = 10}):
\deqn{S_t = \sum_{i = t - w + 1}^{t} P_i}

The Day of the Year (DOY) for each seasonal event is defined by finding the earliest
day \eqn{t} that satisfies specific threshold conditions, adjusted back to the start
of the \eqn{w}-day window:

\strong{Wet Season Onset:}
\deqn{Onset_{wet} = \min(t) - (w - 1) \quad \text{where } S_t \ge W_{th} \text{ and } t > d_{spring}}

\strong{Wet Season Retreat:}
\deqn{Retreat_{wet} = \min(t) - (w - 1) \quad \text{where } S_t < D_{th} \text{ and } t > d_{autumn}}

\strong{Dry Season Onset & Retreat:}
Uses symmetrical logic, isolating periods where \eqn{S_t < D_{th}} during winter months
and \eqn{S_t \ge W_{th}} during the spring transition.
}
